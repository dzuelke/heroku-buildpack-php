# heroku-buildpack-php support files

This directory contains ancillary infrastructure necessary to support operation of the buildpack.

## "build"

All tooling for building binaries is located here.

## "formulae"

The actual build scripts for web servers, runtimes and extensions ("platform packages").

## "installer"

The custom Composer installer used to install platform packages.
